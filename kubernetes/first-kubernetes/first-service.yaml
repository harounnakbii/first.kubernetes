apiVersion: v1
kind: Service
metadata:
  name: first-kubernetes-service
  labels:
    app: first-kubernetes
# Just for internal using
spec:
  type: ClusterIP
  selector:
    app: first-kubernetes
  ports:
    - name: http
      port: 8080
      targetPort: 8080
# spec:
#   type: NodePort
#   selector:
#     app: first-kubernetes
#   ports:
#     - name: http
#       protocol: TCP
#       port: 8080
#       targetPort: 8080
#       nodePort: 30080
